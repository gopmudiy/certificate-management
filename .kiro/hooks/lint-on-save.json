{
  "name": "Lint on Save",
  "version": "1.0.0",
  "description": "Runs ruff linter when Python files are saved",
  "when": {
    "type": "fileEdited",
    "patterns": ["*.py"]
  },
  "then": {
    "type": "runCommand",
    "command": "ruff check --fix ${file}"
  }
}
