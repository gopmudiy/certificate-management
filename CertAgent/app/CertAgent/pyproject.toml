[project]
name = "certagent"
version = "1.0.0"
description = "AI-powered certificate lifecycle management agent"
requires-python = ">=3.12"
dependencies = [
    "strands-agents",
    "strands-agents-bedrock",
    "boto3>=1.35.0",
]
